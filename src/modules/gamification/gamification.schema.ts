// No request bodies needed yet — GET /gamification/me takes no query
// params, POST /gamification/checkin takes no body. Kept as a file (rather
// than omitted) to match the four-file module shape used everywhere else
// in this project.
export {};
